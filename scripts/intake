#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"

if [ -x "${REPO_ROOT}/.venv/bin/python" ]; then
  exec "${REPO_ROOT}/.venv/bin/python" -m intake_skill "$@"
fi

exec python -m intake_skill "$@"
